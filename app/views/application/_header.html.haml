%header#header
  %h1.left{:style => "margin-right: 15px"} App²
  %h1.left{:style => "margin-right: 15px"} -
  %h2.left Le Application App
  %br.clear

  - if user_signed_in?
    = link_to "Logout", destroy_user_session_path, :method => :delete